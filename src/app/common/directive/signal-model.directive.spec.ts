import { SignalModelDirective } from './signal-model.directive';

describe('SignalModelDirective', () => {
  it('should create an instance', () => {
    const directive = new SignalModelDirective();
    expect(directive).toBeTruthy();
  });
});
